<?xml version="1.0"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="opensource.giangtrinh.fgsv_test"
    android:installLocation="auto"
    android:versionCode="-- %%INSERT_VERSION_CODE%% --"
    android:versionName="-- %%INSERT_VERSION_NAME%% --">
<!--manifest modified properties
    package="org.qtproject.example" -> package="opensource.giangtrinh.fgsv_test"
        note:
            - package is uni path for each app. change
            package will cause install new app on android
            Then we can see 2 app old and new on launcher.
            - package is also using as application path
            to generate R class contain project resources
-->


    <!-- %%INSERT_PERMISSIONS -->

<!-- users-permission appended
    FOREGROUND_SERVICE,
    FOREGROUND_SERVICE_DATA_SYNC // services:for .NotificationClient
-->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"/>



    <!-- %%INSERT_FEATURES -->
    <supports-screens
        android:anyDensity="true"
        android:largeScreens="true"
        android:normalScreens="true"
        android:smallScreens="true" />
    <application
        android:name="org.qtproject.qt.android.bindings.QtApplication"
        android:hardwareAccelerated="true"
        android:label="-- %%INSERT_APP_NAME%% --"
        android:requestLegacyExternalStorage="true"
        android:allowNativeHeapPointerTagging="false"
        android:allowBackup="true"
        android:fullBackupOnly="false"
        android:icon="@mipmap/sunset">
<!--  application appended properties:
icon mipmap -->

        <activity
            android:name="org.qtproject.qt.android.bindings.QtActivity"
            android:configChanges="orientation|uiMode|screenLayout|screenSize|smallestScreenSize|layoutDirection|locale|fontScale|keyboard|keyboardHidden|navigation|mcc|mnc|density"
            android:label="-- %%INSERT_APP_NAME%% --"
            android:launchMode="singleTop"
            android:screenOrientation="unspecified"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

          <meta-data
                android:name="android.app.lib_name"
                android:value="-- %%INSERT_APP_LIB_NAME%% --" />

          <meta-data
                android:name="android.app.arguments"
                android:value="-- %%INSERT_APP_ARGUMENTS%% --" />

            <meta-data
                android:name="android.app.extract_android_style"
                android:value="minimal" />
        </activity>


<!-- Add Service:
    android:exported="true" -> open to receive Intent from other process.
        Sometime using while run service in independent process

    NotificationClient:
        - add service type is dataSync, other type see:
            https://developer.android.com/develop/background-work/services/fgs/service-types
        - set argument by call the main() function twice while boot service:
            https://doc.qt.io/qt-6.5/android-services.html#use-the-same-so-lib-file

    QtAndroidService: same

Notice:
    - make service run in another process, add property below:
        android:process=":qt_service"
            <- this is thread name for service
            <- can change and find pid by: adb shell pidof opensource.giangtrinh.fgsv_test:qt_service
    - the addition tag below inside `service` only useful while run service in another process:
        (comment) Application arguments
        <meta-data android:name="android.app.arguments" android:value="-service-notify"/>

        <meta-data android:name="android.app.lib_name" android:value="...
        (pls find remain code at: https://doc.qt.io/qt-6.5/android-services.html)

-->
        <service android:name="opensource.giangtrinh.fgsv_test.src.QtAndroidService" android:exported="false">
            <!-- Background running -->
            <meta-data android:name="android.app.background_running" android:value="true"/>
            <!-- Background running -->
        </service>

        <service
            android:process=":qt_notify_service"

            android:name="opensource.giangtrinh.fgsv_test.src.NotificationClient" android:exported="false"
            android:foregroundServiceType="dataSync" >

            <!-- Background running -->
            <meta-data android:name="android.app.background_running" android:value="true"/>
            <!-- Background running -->

            <!--Application arguments-->
            <meta-data android:name="android.app.arguments" android:value="-service-notify"/>
            <!--Application arguments-->
            <meta-data android:name="android.app.lib_name" android:value="-- %%INSERT_APP_LIB_NAME%% --"/>

        </service>



    </application>
</manifest>
